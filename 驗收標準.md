# 驗收標準 — RK3568 Rust 語音服務器 (ASR + TTS)

> 每項測試需記錄：通過 / 失敗 / 無法測試（附原因）

---

## A0：環境準備

| 編號 | 檢查項目 | 預期結果 | 狀態 |
|---|---|---|---|
| A0.1 | Cross-compilation 環境 | `cargo build --target aarch64-unknown-linux-gnu --release` 成功 | [ ] |
| A0.2 | 編譯結果 | 產出單一靜態連結二進制檔 < 50 MB | [ ] |
| A0.3 | 最小範例驗證 | 在 RK3568 上執行最小 ASR 範例能正確輸出辨識文字 | [ ] |

---

## A1：ASR 功能

| 編號 | 檢查項目 | 預期結果 | 狀態 |
|---|---|---|---|
| A1.1 | WebSocket 連線 | 客戶端可成功連線 `ws://<ip>:8080/ws` | [ ] |
| A1.2 | ASR 基本辨識 | 傳送 5 秒英文語音 → 5 秒內收到 `asr_final`，文字正確率 > 70% | [ ] |
| A1.3 | ASR 中間結果 | 傳送長語音期間收到至少 1 次 `asr_interim` | [ ] |
| A1.4 | ASR 最終結果信心度 | `asr_final` 包含 `confidence` 欄位 (0.0 ~ 1.0) | [ ] |
| A1.5 | 中文 ASR | 傳送中文語音 → 收到正確中文字串 | [ ] |
| A1.6 | ASR 停止訊號 | 傳送 `asr_stop` → ASR session 結束，回傳最終結果 | [ ] |
| A1.7 | 二進制音訊傳輸 | 使用 binary WebSocket frame 傳送音訊可正常辨識 | [ ] |

---

## A2：VAD 功能

| 編號 | 檢查項目 | 預期結果 | 狀態 |
|---|---|---|---|
| A2.1 | VAD 狀態回報 | 說話時收到 `vad_state: "speech"`，靜音時收到 `silence` | [ ] |
| A2.2 | VAD 自動 endpoint | 靜音持續 > 500ms 後自動結束 ASR session 並回傳 `asr_final` | [ ] |
| A2.3 | VAD 敏感度配置 | 調整 `threshold` 參數可改變偵測敏感度 | [ ] |

---

## A3：TTS 功能

| 編號 | 檢查項目 | 預期結果 | 狀態 |
|---|---|---|---|
| A3.1 | TTS 基本合成 | 傳送英文文字 → 1 秒內收到第一個 `tts_audio` chunk | [ ] |
| A3.2 | TTS streaming | 收到多個 `tts_audio` chunks（非一次性全部） | [ ] |
| A3.3 | TTS 結束通知 | 合成完成後收到 `tts_end`，含 `duration_ms` | [ ] |
| A3.4 | TTS 音訊品質 | 合成的語音可聽懂，MOS > 3.0（主觀認定） | [ ] |
| A3.5 | TTS 中文 | 傳送中文字串 → 合成中文語音 | [ ] |
| A3.6 | TTS 取消 | 傳送 `tts_cancel` → TTS 合成中斷，不再收到音訊 | [ ] |

---

## A4：多路併發

| 編號 | 檢查項目 | 預期結果 | 狀態 |
|---|---|---|---|
| A4.1 | 雙路 ASR | 2 個客戶端同時 ASR，兩路皆可正常辨識 | [ ] |
| A4.2 | 雙路 TTS | 2 個客戶端同時 TTS，兩路皆正常合成 | [ ] |
| A4.3 | ASR + TTS 混合 | 一路 ASR 同時另一路 TTS，互不影響 | [ ] |
| A4.4 | 4 路併發 | 4 個客戶端同時連線皆正常運行 | [ ] |

---

## A5：效能與資源

| 編號 | 檢查項目 | 預期結果 | 狀態 |
|---|---|---|---|
| A5.1 | ASR RTF | 單路串流 ASR 的 Real-Time Factor < 0.5 | [ ] |
| A5.2 | TTS 首次 chunk 延遲 | 從請求到收到第一個 chunk < 500 ms | [ ] |
| A5.3 | 記憶體（閒置） | 服務器啟動後 RSS < 200 MB（不含模型 mmap） | [ ] |
| A5.4 | 記憶體（滿載 4 路） | RSS < 1 GB | [ ] |
| A5.5 | CPU（閒置） | 無連線時 CPU < 5% | [ ] |
| A5.6 | CPU（1 路 ASR） | 平均 CPU 使用率 < 50%（單核） | [ ] |
| A5.7 | 長時間穩定 | 連續運行 1 小時，無記憶體增長趨勢 | [ ] |

---

## A6：部署與管理

| 編號 | 檢查項目 | 預期結果 | 狀態 |
|---|---|---|---|
| A6.1 | 一鍵安裝 | `install.sh` 可完成全部部署 | [ ] |
| A6.2 | systemd service | `systemctl start voice-server` 正常啟動 | [ ] |
| A6.3 | 開機自啟 | `systemctl enable voice-server` → 重啟後服務自動運行 | [ ] |
| A6.4 | 設定檔生效 | 修改 `config.toml` 的 port → 重啟後 port 變更 | [ ] |
| A6.5 | 優雅關閉 | `systemctl stop voice-server` → 5 秒內正常終止 | [ ] |
| A6.6 | 健康檢查 | `GET /health` 回傳 HTTP 200 + `{"status": "ok"}` | [ ] |
| A6.7 | 斷線清理 | 客戶端異常斷線 → 服務器釋放對應資源（無記憶體洩漏） | [ ] |

---

## A7：安全性（基本）

| 編號 | 檢查項目 | 預期結果 | 狀態 |
|---|---|---|---|
| A7.1 | 輸入驗證 | 超大 audio chunk (> 10 MB) → 拒絕並回傳 error | [ ] |
| A7.2 | 連線上限 | 超過 `max_connections` → 拒絕新連線 | [ ] |
| A7.3 | 錯誤不回崩潰 | 任何客戶端異常行為不應導致服務器 crash | [ ] |

---

## 驗收結論

| 等級 | 標準 | 結果 |
|---|---|---|
| ✅ **通過** | A1~A3 全部 P0 項目通過，且 A5 主要項目通過 | [ ] |
| ⚠️ **有條件通過** | A1~A3 P0 全通，但 A5 部分未達標（附理由與改善計畫） | [ ] |
| ❌ **不通過** | A1~A3 任一 P0 項目失敗 | [ ] |

---

## 測試環境記錄

| 項目 | 內容 |
|---|---|
| 測試日期 | |
| RK3568 板型 | |
| Armbian 版本 | |
| 記憶體大小 | |
| 模型組合 (ASR/TTS/VAD) | |
| 測試人員 | |
